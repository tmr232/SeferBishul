<!DOCTYPE html>
<html lang="he" dir="rtl">

<head>
  <meta charset="UTF-8" />
  <title>ספר המתכונים של נמיר</title>
  <style>
    @media only screen and (max-width: 1000px) {
      body {
        font-size: 2.5vmax;
      }
    }

    @media print,
    screen and (min-width: 1001px) {
      body {}

      .recipe-body {
        display: grid;
        grid-template-columns: 1fr 3fr;
      }

      .ingredients {
        line-height: 1.3;
        font-size: 0.8em;
        padding-left: 2em;
        border-left: 1px solid black;
      }

      .ingredients li {
        list-style: none;
        padding-right: 2em;
        text-indent: -2em;
      }

      .ingredients ul {
        padding-right: 0;
      }

      .prep {
        margin-right: 2em;
      }
    }

    .toc {
      line-height: 1.5em;
    }

    a {
      color: black;
      text-decoration: none;
    }

    .toc a:hover {
      text-decoration: underline;
    }

    .gallery {
        display: flex;
        flex-direction: row;
        flex-wrap: wrap;
    }

    .picture {
        padding: 20px;
        width: 400px;
        height: 400px;
        overflow: clip;
        a {
        display: block;
}       div {
border-radius: 10px;
overflow: clip;
filter: drop-shadow(5px 5px 5px black);
max-height: 300px;
}
    }
     .picture img {
        width: 100%;
        display: block;
    }
  </style>
</head>
<body>
  <h1>ספר המתכונים של נמיר</h1>
  <div class="gallery">
  {% for recipe in recipes %}
  {% if recipe.hero %}
  <div class="picture">
  <a href="{{ recipe.link }}">
    <h2>{{ recipe.name }}</h2>
    <div>
    <img src="{{ recipe.hero }}"/>
    </div>
    </a>
  </div>
  {% endif %}
  {% endfor %}
  </div>
</body>

</html>