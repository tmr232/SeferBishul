<!DOCTYPE html>
<html lang="he" dir="rtl">

<head>
  <meta charset="UTF-8" />
  <title>ספר המתכונים של נמיר</title>
  <style>
    @media only screen and (max-width: 1000px) {
      body {
        font-size: 2.5vmax;
      }
    }

    @media print,
    screen and (min-width: 1001px) {
      body {}

      .recipe-body {
        display: grid;
        grid-template-columns: 1fr 3fr;
      }

      .ingredients {
        line-height: 1.3;
        font-size: 0.8em;
        padding-left: 2em;
        border-left: 1px solid black;
      }

      .ingredients li {
        list-style: none;
        padding-right: 2em;
        text-indent: -2em;
      }

      .ingredients ul {
        padding-right: 0;
      }

      .prep {
        margin-right: 2em;
      }
    }

    .toc {
      line-height: 1.5em;
    }

    .toc a,
     h1 a {
      color: black;
      text-decoration: none;
    }

    .toc a:hover {
      text-decoration: underline;
    }
  </style>
</head>
{% macro list_recipe(recipe) -%}
<li><a href="{{ recipe.link }}">{{ recipe.name }}</a></li>
{%- endmacro %}

<body>
  <h1>ספר המתכונים של נמיר<a href="/picture-menu.html">🖼️</a></h1>

  <div class="toc">
    <ul>
      {% for recipe in toc %}
      {% if recipe.is_group %}
    </ul>
    <h2 class="category">{{ recipe.name }}</h2>
    <ul>
      {% for recipe in recipe.recipes %}
      {{ list_recipe(recipe) }}
      {% endfor %}
    </ul>
    {% else %}
    {{ list_recipe(recipe) }}
    {% endif %}
    {%endfor%}
    </ul>

  </div>
</body>

</html>